# Luyumi Launcher Environment Configuration
# IMPORTANT: This is an example file. Create a .env file with actual values.
# 
# SECURITY WARNING:
# - .env file contains sensitive information
# - NEVER commit .env to version control
# - NEVER include .env in release builds
# - In release builds, use encrypted configuration or environment variables only
# - All sensitive values should be loaded from secure backend APIs

# API Configuration
BACKEND_HOST=localhost
BACKEND_PORT=8080
API_TIMEOUT=10

# Auth Configuration (loaded from backend, these are fallbacks only)
AUTH_DOMAIN=sanasol.ws
AUTH_SERVER=https://sessions.sanasol.ws

# Feature Flags
DEBUG_MODE=false
LOG_LEVEL=info

# For development only
DEVELOPMENT_MODE=false

# CurseForge API Configuration
# Get your API key from: https://console.curseforge.com/
# This is required for mod search functionality
CURSEFORGE_API_KEY=your_api_key_here
